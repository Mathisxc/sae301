<!DOCTYPE html>
<!-- Coding By CodingNepal - www.codingnepalweb.com -->
<html lang="fr" dir="ltr">
  <head>
    <meta charset="utf-8">
    <title>Messages</title>
    <link rel="stylesheet" href="{{ asset('css/stylechat.css') }}">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <!-- Google Fonts Link For Icons -->
    <link rel="stylesheet" href="https://fonts.googleapis.com/css2?family=Material+Symbols+Rounded:opsz,wght,FILL,GRAD@48,400,1,0" />
    <script src="{{ asset('js/scriptchat.js') }}" defer></script>
  </head>
  <body>
    <button class="chatbot-toggler">
      <span class="material-symbols-rounded">mode_comment</span>
      <span class="material-symbols-outlined">fermer</span>
    </button>
    <div class="chatbot">

        <header>
            <img src="{{ asset('svg/logo3.svg') }}" alt="Local Logo" id="logo" height="40px" border-color="black">
            <h2>Professeur</h2>
            <span class="close-btn material-symbols-outlined">close</span>
        </header>
        
      <ul class="chatbox">
        <li class="chat incoming">
          <span class="material-symbols-outlined"><img src="{{ asset('img/logo1.png') }}" alt="Local Logo" id="logo" class="logochat" height=20px margin-top="5px"> </span>
          <p>Salut 👋 <br>Comment puis-je vous aider aujourd'hui?</p>
        </li>
      </ul>
      <div class="chat-input">
        <textarea placeholder="Entrez un message ..." spellcheck="false" required></textarea>
        <span id="send-btn" class="material-symbols-rounded">send</span>
      </div>
    </div>

    {# You can include your script directly in the Twig template if needed #}
    <script>
        function addIncomingMessage(message) {
            const chatbox = document.querySelector(".chatbox");
            const newMessage = document.createElement("li");
            newMessage.classList.add("chat", "incoming");

            // Remplacez le logo du robot par l'image locale
            const logoImage = document.createElement("img");
            logoImage.src = "{{ asset('svg/logo2.svg') }}";
            logoImage.alt = "Logo";
            newMessage.appendChild(logoImage);

            const messageParagraph = document.createElement("p");
            messageParagraph.innerHTML = message;
            newMessage.appendChild(messageParagraph);

            chatbox.appendChild(newMessage);
        }
    </script>

  </body>
</html>
